<h2>Available drones</h2>
{{#each drones}}
    <p>Drone name: {{name}}</p>
    <p>Number of propellers: {{propellers}}</p>
    <p>Max speed: {{maxSpeed}}mph</p>
    <a href="/drones/{{_id}}/edit">Edit drone</a>
    <form action="/drones/{{_id}}/delete" method="post">
        <button>Destroy drone</button>
    </form>
{{/each}}